export const findProducts = (reqData) => async (dispatch) => {
    const {capacity, color, minPrice, maxPrice, minDiscount, category, stock, sort, pageNumber, pageSize} 
    try {
        
    } catch (error) {
        
    } 
}