export const findProducts = (reqData) => async (dispatch) => {
    const {capacity, color, minPrice, maxPrice, minDiscount, category, stock, } 
    try {
        
    } catch (error) {
        
    } 
}